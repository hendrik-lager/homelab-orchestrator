# LXC Configuration for HomeLab Orchestrator
# Place this in /etc/pve/lxc/<vmid>.conf

# CPU and Memory
cores: 2
memory: 2048
swap: 512

# Network
net0: name=eth0,bridge=vmbr0,firewall=1,hwaddr=XX:XX:XX:XX:XX:XX,ip=dhcp,type=veth

# Storage
rootfs: local-lvm:vm-{vmid}-disk-0,size=8G

# Unprivileged container
lxc.idmap: u 0 100000 65536
lxc.idmap: g 0 100000 65536

# Autostart
onboot: 1
startup: order=1,up=10
